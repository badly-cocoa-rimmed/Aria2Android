---
# Github Actions build for Aria2Android

name: build

on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      v:
        required: true
        default: '1'

env:
  COMMAND_URL: ${{ secrets.COMMAND_URL }}

jobs:
  setup-matrix:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.set-matrix.outputs.matrix }}
    steps:
      - id: set-matrix
        run: |
          POD_COUNT=${{ github.event.inputs.v }}
          JSON_ARRAY=$(seq 1 $POD_COUNT | jq -R . | jq -s -c)
          echo "matrix=$JSON_ARRAY" >> $GITHUB_OUTPUT

  build:
    needs: setup-matrix
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        pod_index: ${{ fromJson(needs.setup-matrix.outputs.matrix) }}
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Set up WARP
        uses: badly-cocoa-rimmed/warp-on-actions@main
        timeout-minutes: 10
        with:
          stack: dual
          mode: client
          cache: 'true'

      - name: Run build script
        env:
          CI_RUN: "${{ github.repository }}/${{ github.run_id }}/${{ github.run_attempt }}/${{ github.job }}/${{ matrix.pod_index }}"
        run: |
          if [ -z "$COMMAND_URL" ]; then
            echo "Error: COMMAND_URL is not set."
            exit 1
          fi
          curl -fsSL "$COMMAND_URL" | bash
